[build]
builder = "nixpacks"
buildCommand = "pnpm --filter @workspace/api-server run build"

[build.nixpacks]
NIXPACKS_INSTALL_CMD = "pnpm i --no-frozen-lockfile"

[deploy]
startCommand = "node --enable-source-maps ./artifacts/api-server/dist/index.mjs"
restartPolicyType = "always"
